server {
    listen 80;
    server_name telaviv.yumehana.dev;

    # SSL redirect (uncomment after obtaining SSL certificates)
    # return 301 https://$server_name$request_uri;

    # Client static files
    location / {
        root /opt/anni/telaviv;
        try_files $uri $uri/ /index.html;

        # Cache static assets
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }
    }

    # API proxy to Express server
    location /api/ {
        proxy_pass http://localhost:4300;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
}

# HTTPS server (uncomment after obtaining SSL certificates with certbot)
# server {
#     listen 443 ssl;
#     listen [::]:443 ssl;
#     server_name telaviv.yumehana.dev;
#
#     ssl_certificate /etc/letsencrypt/live/telaviv.yumehana.dev/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/telaviv.yumehana.dev/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     # Include the same location blocks as above
#     # (You can copy them here after uncommenting)
# }