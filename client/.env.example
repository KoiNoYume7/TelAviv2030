# ── Client environment ──
# API base URL used by the mobile app and production builds.
# For local development the Vite proxy uses /api.
VITE_API_BASE=https://telaviv.yumehana.dev/api
VITE_APP_VERSION=0.1.0
